


class MoveManage {
    constructor(obj, obj_field, map_field) {
        this.obj = obj
        this.obj_field = obj_field,
        this.map_field = map_field
    }

    move(side) {
        switch (side){
            case 'right':
                
                break;
        }
    }
}